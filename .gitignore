# ── Secrets (NEVER commit — claude-ns-hub is a PUBLIC repo) ──────────────
.telegram-token
.telegram-chat-id
.ntfy-topic
.hub-consent.json
config.yaml
certs/
*.key
*.crt
*.pem

# ── Core implementation (distributed via PyPI only — M1064) ──────────────
server.py
install-hooks.sh
task-worker.sh
entity/
scripts/
hooks/
corpus/
corpora/

# ── Frontend assets (distributed via PyPI only) ──────────────────────────
static/
static/claude-md-preview.html

# ── Internal data/docs ────────────────────────────────────────────────────
dataset/
docs/
ARCHITECTURE.md

# ── Data-pipeline tooling — keep internal ─────────────────────────────────
hub-dataset-export.py
hub-dataset-stats.py
hub-windows-install.bat

# ── Runtime data ─────────────────────────────────────────────────────────
*.db
*.db-shm
*.db-wal
ns-events.db
projects/
uploads/
causality-dataset/
ctx-dashboard/
usage-stats.jsonl
northstar.json
dataset.jsonl
ctx-session-state.json
global-memo.txt
worker.log

# ── Runtime state / locks / logs ─────────────────────────────────────────
task-locks/
task-queue/
task-results/
*.log
_archive-from-claude-*/

# ── Caches / build / browser dumps ───────────────────────────────────────
__pycache__/
*.pyc
build/
dist/
*.egg-info/
.playwright-mcp/
.playwright-hook.json

# ── Backups ──────────────────────────────────────────────────────────────
*.bak
pyproject.toml.bak*
backup-*/
backup-nomcp-*/
backup-terminal-*/
*.pre-mcp-*

# ── Runtime state ─────────────────────────────────────────────────────────
.tg-last-sent.json
ttyd-launch.sh
ttyd-ports/

# ── Private/internal ──────────────────────────────────────────────────────
.omc/
